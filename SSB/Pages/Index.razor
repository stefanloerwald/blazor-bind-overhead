@page "/"

This web page demonstrates how much overhead implicit rendering on events and bind there is in Blazor.
Navigate to the <code>Trivial</code> or <code>Complex</code> pages and wait for it to render completely.
Then try interacting with the page by clicking the buttons.